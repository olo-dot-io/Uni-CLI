# Acceptance Ledger

## Broad AI primary-source intelligence — 2026-07-17

| Acceptance                                                                                                                                 | Disproving check                                                                                                          | Status                                                                                                                                    |
| ------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| Practitioners can inspect what each role monitors daily.                                                                                   | `unicli ai profiles -f json` lacks any of the ten role records or daily concerns.                                         | Passed — the built CLI returned all 10 role records.                                                                                      |
| Agents can inspect a broad first-party target directory.                                                                                   | `unicli ai landscape -f json` returns fewer than 100 maintained targets or mislabels generic community posts as official. | Passed — the built CLI returned 100 targets; attribution and catalog invariants are unit-locked.                                          |
| World-model, inference, training, hardware, agent, evaluation, and research searches use role-specific live sources.                       | `ai search --profile` ignores the profile when selecting source refs or official domains.                                 | Passed — profile source/domain selection and unsupported profile behavior are unit-locked.                                                |
| ModelScope, OpenCSG, Bluesky posts, scholarly indexes, talks, and opt-in regional/social communities are federated through owned adapters. | `ai sources` omits their `ai.*` capability rows or a live public adapter cannot return structured data.                   | Passed — 35 capability rows were discovered; live public probes returned structured ModelScope, OpenCSG, and Bluesky records.             |
| A current pulse retains provenance, source timestamps, failures, and explicit auth boundaries.                                             | `ai pulse` invents timestamps, silently includes cookie sources, or discards partial failures.                            | Passed — live world-model and inference pulses retained timestamps, provenance, structured partial failures, and explicit auth exclusion. |
| CLI and MCP expose the same six `ai` commands.                                                                                             | Manifest, expanded/deferred MCP, or default `unicli_run` lacks a registered command.                                      | Passed — generated manifest, CLI transport, MCP transport, and adapter tests agree on all six commands.                                   |

## Agent dogfood retrieval hardening — 2026-07-17

| Acceptance                                                                         | Disproving check                                                                                                                        | Status                                                                                                                                                                                   |
| ---------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Exact domestic accelerator queries do not inherit unrelated hardware domains.      | A T-Head/Kunlunxin/Cambricon query selects AMD, Furiosa, X, or arbitrary profile domains.                                               | Passed — exact-domain unit locks passed; live T-Head search returned five T-Head-owned rows.                                                                                             |
| PDF bytes never become successful Markdown.                                        | NVIDIA, arXiv, or Cambricon PDF content begins `%PDF-` while `ok=true` and `content_format=markdown`.                                   | Passed — MIME/magic unit locks passed; live NVIDIA PDF returned 7,345 characters of `pdf-text` for pages 1–2.                                                                            |
| Browser-verification pages fail closed.                                            | OpenReview verification content is returned as an official paper.                                                                       | Passed — unit lock passed; live OpenReview read returned `challenge_required`.                                                                                                           |
| GitHub issue/PR reads include the structured upstream thread.                      | `ai read` returns only body/comment count and omits comments or PR review state.                                                        | Passed — registered thread adapters passed lint/tests; live NVIDIA/nccl#1672 read returned all 16 comments.                                                                              |
| Hosting platform is distinct from artifact maintainer.                             | A Hugging Face paper mirror or ModelScope/OpenCSG model is labeled platform-maintained official content.                                | Passed — provenance unit locks classify these as `hosted-artifact` and retain `hosting_platform`.                                                                                        |
| Deep technical vocabulary discovers the unified AI boundary.                       | NVLink, T-Head, Kunlunxin, Cambricon, PagedAttention, RoPE, YaRN, or world-model queries return unrelated office commands or no result. | Passed — three discovery regression families find `ai/search` in the top five.                                                                                                           |
| The change satisfies the repository execution contract.                            | Typecheck, lint, unit, adapter schema, build, or full verification fails.                                                               | Passed — full `npm run verify` completed: 3,092 unit, 94 integration, 6,527 adapter, performance, coverage, stats, truth, conformance, export, changeset, and boundary gates all passed. |
| Vendor-scoped GitHub discovery preserves catalog-owned repositories.               | `openC910 --vendors alibaba-thead` becomes empty because a marketing term is injected or official repository ownership is lost.         | Passed — an engine-level fake-gh regression and a live query both returned `XUANTIE-RV/openc910` as official.                                                                            |
| Reader deadlines reach actual TypeScript adapter work.                             | The outer 60-second deadline does not reach PDF download or `pdftotext`.                                                                | Passed — kernel context, nested download, and extraction signal propagation are code- and unit-locked.                                                                                   |
| Text/binary classification accepts AI manifests without decoding binary artifacts. | YAML/TOML/NDJSON are rejected, or Office/PDF/ZIP/WASM data is accepted as text.                                                         | Passed — positive structured-text and negative MIME/magic regression matrices passed.                                                                                                    |
| Independent implementation audit has no unresolved high-severity defect.           | The reviewer reports any P0/P1 after repair.                                                                                            | Passed — final re-review reported no P0/P1.                                                                                                                                              |

## v0.400.1 package parity — 2026-07-18

| Acceptance                                                                         | Disproving check                                                                                                                | Status                                                                                                                                                                                                                                                                                                             |
| ---------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Production installs load every generic retrieval source visible in the build.      | Pack and install the tarball without dev dependencies, then compare `retrieval sources` counts.                                 | Passed — the repaired package returned all 41 sources, including `pubmed.search`; the pre-fix package returned only 40.                                                                                                                                                                                            |
| Literal runtime package loads cannot depend on accidental development transitives. | Remove the XML DOM dependency and run `npm run truth:check`; exercise the checker's built-in syntax matrix.                     | Passed — the symbol-aware TypeScript gate failed on the PubMed and bioRxiv paths; its self-check covers runtime import/export, dynamic import, createRequire, require.resolve, late aliases, assignment, resolver destructuring, templates, type-only declarations, lexical shadows, and mutable-loader rejection. |
| Browser broker auto-start never needs a development transpiler in production.      | Search runtime package loads for `tsx`, build, and execute the broker auto-start integration.                                   | Passed — focused source-mode (11/11) and lifecycle (4/4) suites passed; the clean production install spawned protocol v5 from the compiled artifact with zero providers and no Chrome side effect.                                                                                                                 |
| Root lock dependency maps exactly match the publication manifest.                  | Add, remove, or change a root dependency in only one of `package.json` and `package-lock.json`, then run `npm run truth:check`. | Passed — a stale root optional-dependency fixture failed with the exact mismatch; the repaired manifest/lock passed the complete truth gate, fresh install, and package build.                                                                                                                                     |
| The release artifact executes a real adapter requiring the repaired closure.       | Run a PubMed query from a fresh tarball installation with development dependencies omitted.                                     | Passed — the installed 0.400.1 candidate returned one current structured PubMed result.                                                                                                                                                                                                                            |
| The final candidate satisfies all repository and publication-local gates.          | Re-run verify, production audit, strict release check, docs check, and npm publish dry-run.                                     | Passed — the final candidate passed complete verification, zero-vulnerability production audit, 29/29 strict release checks, 150-file public-doc validation, npm publish dry-run, and clean production installation.                                                                                               |
