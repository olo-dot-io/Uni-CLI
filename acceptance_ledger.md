# Acceptance Ledger

## Broad AI primary-source intelligence — 2026-07-17

| Acceptance                                                                                                                                 | Disproving check                                                                                                          | Status                                                                                                                                    |
| ------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| Practitioners can inspect what each role monitors daily.                                                                                   | `unicli ai profiles -f json` lacks any of the ten role records or daily concerns.                                         | Passed — the built CLI returned all 10 role records.                                                                                      |
| Agents can inspect a broad first-party target directory.                                                                                   | `unicli ai landscape -f json` returns fewer than 100 maintained targets or mislabels generic community posts as official. | Passed — the built CLI returned 100 targets; attribution and catalog invariants are unit-locked.                                          |
| World-model, inference, training, hardware, agent, evaluation, and research searches use role-specific live sources.                       | `ai search --profile` ignores the profile when selecting source refs or official domains.                                 | Passed — profile source/domain selection and unsupported profile behavior are unit-locked.                                                |
| ModelScope, OpenCSG, Bluesky posts, scholarly indexes, talks, and opt-in regional/social communities are federated through owned adapters. | `ai sources` omits their `ai.*` capability rows or a live public adapter cannot return structured data.                   | Passed — 35 capability rows were discovered; live public probes returned structured ModelScope, OpenCSG, and Bluesky records.             |
| A current pulse retains provenance, source timestamps, failures, and explicit auth boundaries.                                             | `ai pulse` invents timestamps, silently includes cookie sources, or discards partial failures.                            | Passed — live world-model and inference pulses retained timestamps, provenance, structured partial failures, and explicit auth exclusion. |
| CLI and MCP expose the same six `ai` commands.                                                                                             | Manifest, expanded/deferred MCP, or default `unicli_run` lacks a registered command.                                      | Passed — generated manifest, CLI transport, MCP transport, and adapter tests agree on all six commands.                                   |
