site: excel
name: set-font
description: Apply a font to a cell in the active Microsoft Excel workbook
type: desktop
strategy: public
binary: osascript
detect: "test $(uname) = Darwin"

args:
  font:
    type: str
    required: true
    positional: true
    description: Font family to apply
  size:
    type: str
    required: false
    default: ""
    description: Optional font size in points
  cell:
    type: str
    required: false
    default: A1
    description: Cell address on the active sheet

pipeline:
  - exec:
      command: osascript
      args:
        - "-e"
        - |
          on replace_text(theText, searchString, replacementString)
            set AppleScript's text item delimiters to searchString
            set parts to text items of (theText as text)
            set AppleScript's text item delimiters to replacementString
            set outText to parts as text
            set AppleScript's text item delimiters to ""
            return outText
          end replace_text

          on json_string(valueText)
            set s to valueText as text
            set s to my replace_text(s, "\\", "\\\\")
            set s to my replace_text(s, "\"", "\\\"")
            set s to my replace_text(s, return, "\\n")
            set s to my replace_text(s, linefeed, "\\n")
            set s to my replace_text(s, tab, "\\t")
            return "\"" & s & "\""
          end json_string

          set fontName to system attribute "UNICLI_FONT"
          set sizeText to system attribute "UNICLI_SIZE"
          set cellRef to system attribute "UNICLI_CELL"
          if cellRef is "" then set cellRef to "A1"

          try
            with timeout of 10 seconds
              tell application "Microsoft Excel"
                activate
                if (count workbooks) is 0 then make new workbook
                set wb to active workbook
                set sh to active sheet
                set targetRange to range cellRef of sh
                set f to font object of targetRange
                set name of f to fontName
                if sizeText is not "" then set font size of f to (sizeText as real)
                return "[{\"workbook\":" & my json_string(name of wb as text) & ",\"sheet\":" & my json_string(name of sh as text) & ",\"cell\":" & my json_string(cellRef) & ",\"font\":" & my json_string(fontName) & ",\"size\":" & my json_string(sizeText) & "}]"
              end tell
            end timeout
          on error errMsg number errNum
            return "[{\"ok\":false,\"code\":\"office_automation_unavailable\",\"message\":" & my json_string(errMsg) & ",\"number\":" & errNum & "}]"
          end try
      env:
        UNICLI_FONT: "${{ args.font }}"
        UNICLI_SIZE: "${{ args.size }}"
        UNICLI_CELL: "${{ args.cell }}"
      parse: json
      timeout: 15000

columns: [workbook, sheet, cell, font, size]

capabilities: ["subprocess.exec"]
minimum_capability: subprocess.exec
trust: user
confidentiality: private
quarantine: false
schema_version: v2
