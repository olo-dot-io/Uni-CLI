# Desktop adapter — concatenate multiple media files using a list file
site: ffmpeg
name: concat
description: Concatenate multiple media files listed in a text file
type: desktop
binary: ffmpeg
detect: which ffmpeg
strategy: public

args:
  listfile:
    type: str
    required: true
    positional: true
    description: "Text file listing inputs, one per line: file 'path/to/clip.mp4'"
  output:
    type: str
    required: true
    description: Output file path

pipeline:
  - exec:
      command: ffmpeg
      args:
        - "-f"
        - "concat"
        - "-safe"
        - "0"
        - "-i"
        - "${{ args.listfile }}"
        - "-c"
        - "copy"
        - "-y"
        - "${{ args.output }}"
      parse: text
      timeout: 300000

columns: []
