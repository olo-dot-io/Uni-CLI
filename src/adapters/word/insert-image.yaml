site: word
name: insert-image
description: Insert a local image into the active Microsoft Word document
type: desktop
strategy: public
binary: osascript
detect: "test $(uname) = Darwin"

args:
  path:
    type: str
    required: true
    positional: true
    description: Local image path to embed

pipeline:
  - exec:
      command: osascript
      args:
        - "-e"
        - |
          on replace_text(theText, searchString, replacementString)
            set AppleScript's text item delimiters to searchString
            set parts to text items of (theText as text)
            set AppleScript's text item delimiters to replacementString
            set outText to parts as text
            set AppleScript's text item delimiters to ""
            return outText
          end replace_text

          on json_string(valueText)
            set s to valueText as text
            set s to my replace_text(s, "\\", "\\\\")
            set s to my replace_text(s, "\"", "\\\"")
            set s to my replace_text(s, return, "\\n")
            set s to my replace_text(s, linefeed, "\\n")
            set s to my replace_text(s, tab, "\\t")
            return "\"" & s & "\""
          end json_string

          set imagePath to system attribute "UNICLI_IMAGE_PATH"

          try
            do shell script "test -f " & quoted form of imagePath
            with timeout of 10 seconds
              tell application "Microsoft Word"
                activate
                if (count documents) is 0 then make new document
                set d to active document
                set docRange to text object of d
                set endPos to (end of content of docRange) - 1
                if endPos < 0 then set endPos to 0
                set insertionRange to create range d start endPos end endPos
                make new inline picture at insertionRange with properties {file name:imagePath, link to file:false, save with document:true}
                return "[{\"app\":\"Microsoft Word\",\"name\":" & my json_string(name of d as text) & ",\"path\":" & my json_string(imagePath) & ",\"embedded\":true,\"inline_pictures\":" & (count inline pictures of d) & "}]"
              end tell
            end timeout
          on error errMsg number errNum
            return "[{\"ok\":false,\"code\":\"office_automation_unavailable\",\"message\":" & my json_string(errMsg) & ",\"number\":" & errNum & "}]"
          end try
      env:
        UNICLI_IMAGE_PATH: "${{ args.path }}"
      parse: json
      timeout: 15000

columns: [app, name, path, embedded, inline_pictures]

capabilities: ["subprocess.exec"]
minimum_capability: subprocess.exec
trust: user
confidentiality: private
quarantine: false
schema_version: v2
