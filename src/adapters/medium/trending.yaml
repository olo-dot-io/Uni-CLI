site: medium
name: trending
description: Medium trending articles
operation_effect: read
type: web-api
domain: medium.com
strategy: public
lint_listing_detail: skip

args:
  limit:
    type: int
    default: 10

pipeline:
  - fetch:
      url: https://medium.com/trending
      headers:
        Accept: application/json

  - select: payload.references.Post

  - map:
      title: "${{ item.title || '' }}"
      subtitle: "${{ (item.content?.subtitle || '') | truncate(80) }}"
      claps: "${{ item.virtuals?.totalClapCount || 0 }}"
      reading_time: "${{ Math.round(item.virtuals?.readingTime || 0) }}min"
      url: "${{ 'https://medium.com/p/' + item.uniqueSlug }}"

  - limit: ${{ args.limit }}

columns: [title, subtitle, claps, reading_time, url]

# schema-v2 metadata — injected by `unicli migrate schema-v2`
capabilities: ["http.fetch"]
minimum_capability: http.fetch
trust: public
confidentiality: public
quarantine: true
quarantineReason: "legacy trending endpoint returns HTTP 403; use medium feed for topic RSS (2026-08-10)"
schema_version: v2
