site: zhihu
name: native-hot
description: Read the current Zhihu hot list through the official Zhihu CLI
type: bridge
strategy: public
binary: zhihu-cli
detect: which zhihu-cli

args:
  limit:
    type: int
    default: 30
    minimum: 1
    maximum: 30
    description: Number of hot topics

pipeline:
  - exec:
      command: "${{ env.ZHIHU_CLI || (env.LOCALAPPDATA ? env.LOCALAPPDATA + '/ZhihuCLI/current/zhihu-cli.exe' : (env.ZHIHU_CLI_HOME || env.HOME + '/Library/Application Support/zhihu-cli') + '/current/zhihu-cli') }}"
      args: [hot, --limit, "${{ args.limit }}"]
      parse: json
      timeout: 30000

operation_effect: read
execution_operator: native-cli
operation_family: list
auth_requirement: required
capabilities: [subprocess.exec]
executables: [zhihu-cli]
minimum_capability: subprocess.exec
trust: user
confidentiality: public
quarantine: false
schema_version: v2
