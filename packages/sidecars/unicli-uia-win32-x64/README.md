# @zenalexa/unicli-uia-win32-x64

Windows x64 UI Automation sidecar binary for Uni-CLI compute.

This package is installed as an optional dependency of `@zenalexa/unicli`.
Install it manually only when `unicli doctor compute` reports a missing UIA
sidecar.
