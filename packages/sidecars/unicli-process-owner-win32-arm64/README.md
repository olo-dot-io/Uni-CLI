# @zenalexa/unicli-process-owner-win32-arm64

Windows arm64 kill-on-close Job Object supervisor for Uni-CLI. The main package
installs it automatically as an optional platform dependency so cancellation,
timeouts, and failed browser startup cannot leave descendant processes alive.
