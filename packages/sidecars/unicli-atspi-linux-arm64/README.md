# @zenalexa/unicli-atspi-linux-arm64

Linux arm64 AT-SPI sidecar binary for Uni-CLI compute.

This package is installed as an optional dependency of `@zenalexa/unicli`.
Install it manually only when `unicli doctor compute` reports a missing AT-SPI
sidecar.
