---
"@zenalexa/unicli": minor
---

Harden browser-backed adapter execution and the release gate for the next development line. TypeScript adapters can now declare command-level browser, strategy, and auth metadata; browser function commands receive a real page from the shared kernel; Reddit and Linux.do search/listing paths were moved off stale public endpoints; GitHub repository search was added; release propagation, release checks, weekly release automation, and publish automation now require an explicit `Program · Astronaut` codename before tagging or publishing.
